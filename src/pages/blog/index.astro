---
// /blog redirects to the terminal experience with cd ~/blog && ls
// Blog listing is handled by the terminal's filesystem
return Astro.redirect(`/?cmd=${encodeURIComponent('cd ~/blog && ls')}`, 302);
---
